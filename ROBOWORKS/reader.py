# read_to_table(path)
# read_robots(robots_path)
# read_destinations(destinations_path)
# read_packages(packages_path)
# read_tasks(tasks_path, valid_destination_ids, valid_package_ids)
# read_schedules(schedules_path, valid_robot_ids, valid_task_ids)
# read_distances(distances_path)
# validation helpers here
# Examples:
# is_valid_id(...)
# is_valid_zone(...)
# is_valid_int(...)
# is_valid_float(...)
# warning-print helpers to stderr